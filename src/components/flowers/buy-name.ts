/**
 * 도감 상세의 「사러 가기」가 검색어로 쓰는 **대표 이름**.
 *
 * ── 합쳤다 (2026-08-18) ──────────────────────────────────────────────
 * 여기에는 결과 화면(`components/flow/ResultView.tsx` 의 `mainName`)과 **글자 그대로
 * 같은 규칙**이 한 벌 더 서 있었다. 그쪽이 내보내지 않는 파일 내부 함수라 가져다 쓸
 * 길이 없었고, 그래서 두 몸통을 소스로 맞대어 보는 대조 테스트가 그물 노릇을 했다.
 *
 * 지금은 규칙이 `flow/view-format.ts` 한 곳에 있고 두 화면이 그것을 부른다. 이 파일이
 * 남아 있는 이유는 도감 쪽 import 경로를 지키기 위해서다 —
 * `flowers/*` 가 결과 화면 모듈을 직접 아는 것보다 이 한 줄을 거치는 편이,
 * 규칙이 또 옮겨 갈 때 고칠 자리가 하나로 남는다.
 *
 * ⚠ 여기에 몸통을 다시 적지 마라. 규칙(과 그 실측 근거 `docs/partners-research.md` §5)은
 *   전부 `view-format.ts` 에 있다.
 */
export { buySearchName } from '@/components/flow/view-format';
