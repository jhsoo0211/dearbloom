/**
 * 예시 편지 한 통 — **저장소를 거치지 않는 내장 편지**.
 *
 * ── 왜 더미 데이터가 아니라 상수인가 ─────────────────────────────────
 * 편지는 그 브라우저의 localStorage 에만 있다(`src/lib/letters/store.ts` 머리말). 그래서
 * "예시를 미리 넣어 둔다" 는 말은 성립하지 않는다 — 남의 브라우저에 우리가 무언가를 심을
 * 길이 없고, 심을 수 있다 해도 남의 편지 목록에 우리가 쓴 편지가 끼어 있는 것은 이 기능의
 * 약속을 깨는 일이다(목록에 있는 편지는 전부 그 사람이 쓴 것이어야 한다).
 * 그래서 예시는 **저장소 밖에** 둔다. 화면이 이 상수를 그대로 열람 연출에 넘긴다.
 *
 * ⚠ 이 편지는 **어떤 경로로도 저장되지 않는다.**
 *   · 이 파일은 `@/lib/letters/store` 를 import 하지 않는다(그물: `tests/letters/sample.test.ts`).
 *   · 번호(`code`)가 없다 — 번호로 여는 길에 이 편지가 걸리는 일 자체가 없다.
 *   · `id` 는 `'sample'` 고정이다. 저장된 편지의 id 는 UUID 이거나 `letter-…` 로 시작하므로
 *     (`createLetterId`) 이 값과 부딪히지 않는다.
 *
 * ── 문안 ─────────────────────────────────────────────────────────────
 * 서비스가 처음부터 들고 다닌 그 예시다 — §1.5d 의 "오래 고마웠던 친구에게 — 프리지아와
 * '고맙다는 말, 너무 오래 미뤘지'라는 첫 문장을 추천했어요". 랜딩이 말로만 하던 그 편지를
 * 여기서 실제로 열어 본다. 꽃은 프리지아(카탈로그 실존 — 실사·꽃말이 자동으로 붙는다),
 * 대표 꽃말 "새로운 시작과 변함없는 우정" 은 본문에 인용하지 않고 **스미게만** 둔다
 * (꽃말은 편지지 아래에 이미 한 줄로 실린다 — 같은 말을 두 번 하지 않는다).
 *
 * 본문 안에서 예시라는 티를 내지 않는다. 예시임은 열람 화면의 각주 한 줄이 말한다.
 */

import type { Letter } from '@/lib/letters/types';

/** 예시 편지의 식별자 — 저장된 편지와 부딪히지 않는 고정값. */
export const SAMPLE_LETTER_ID = 'sample';

export const SAMPLE_LETTER: Letter = {
  id: SAMPLE_LETTER_ID,
  recipientName: '지수',
  title: '봄에 먼저 피는 꽃',
  body: [
    '고맙다는 말, 너무 오래 미뤘지.',
    '',
    '그해 겨울에 내가 아무 말도 못 하고 있을 때, 너는 이유를 묻지 않고 그냥 옆에 앉아 있어 줬어.',
    '그게 얼마나 큰 일이었는지 그때는 몰랐어. 이제야 알아서, 이렇게 늦은 편지를 써.',
    '',
    '꽃집 앞에서 한참을 서 있다가 이 꽃을 골랐어.',
    '겨울을 다 지나고서야 피는데 향은 제일 먼저 번지더라. 어쩐지 너 같아서 웃었어.',
    '',
    '새로 시작하는 자리에서 문득 겁이 나면, 이 편지를 다시 펴 봐.',
    '그때도 나는 여기 있을게.',
  ].join('\n'),
  flowerId: 'freesia',
  theme: 'gold',
  signature: '오래된 친구가',
  /* 화면에 나가지 않는 칸이다(예시는 목록에 서지 않는다). 그래도 `Letter` 의 모양은
     그대로 지켜 두어, 열람 화면이 예시와 진짜 편지를 구분하지 않게 한다. */
  createdAt: '2026-08-16T09:00:00.000Z',
  updatedAt: '2026-08-16T09:00:00.000Z',
};
